/*1. add this for page pagination active link.*/

.pagination .current .page-link {
    z-index: 1;
    color: var(--white);
    background-color: var(--primary);
    border-color: var(--primary);
}

/*2. Post pagination styles.*/

.post-navigation .nav-links {
    display: flex;
    justify-content: space-between;
}

/*3. Display logo on center on mobile.*/

@media (min-width: 768px) {
.navbar-brand
    {
        position: absolute;
        left: 50%;
        transform: translateX(-50%);
        top: -10px;
    }
}